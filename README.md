# ML-Regression
The problem regarding multiple linear regression

## install necessary libraries

* ```pip install numpy```
* ```pip install pandas```
* ```pip install matplotlib```
* ```pip install seaborn```

